id: "AILE-0011"
title: "Record CLI 0.2.0 detached ledger behavior and docs"
date: "2026-03-06"
author: "codex"

intent:
  summary: "Add an AI Ledger contract+entry for the current PR so CI passes and the change is properly recorded."
  reason: "Repository CI enforces that each PR introduces at least one new contract and one new entry under .ai-ledger/; this PR currently lacks new ledger records even though it changes CLI/spec/docs."
  risk_level: "low"

scope:
  expected:
    - ".ai-ledger/contracts/**"
    - ".ai-ledger/entries/**"
    - ".ai-ledger/README.md"
    - "AGENTS.md"
    - "README.md"
    - "packages/cli/**"
    - "spec/v0.1.md"
  forbidden:
    - "infra/**"
    - "auth/**"
    - "supabase/migrations/**"

verification:
  expected_tests:
    - "CI: ai-ledger check for new contract/entry passes"
  manual_checks:
    - "Confirm new files exist in both .ai-ledger/contracts and .ai-ledger/entries and are included in the PR diff."

review:
  requires_human_approval: false

links:
  entry: ".ai-ledger/entries/2026-03-06-pr-ledger-record.md"
  pr: ""
  issue: ""

